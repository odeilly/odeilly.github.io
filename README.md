# MkDocs

## パッケージインストール

### Linux / Mac など

```sh
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### Windows

```sh
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
```
